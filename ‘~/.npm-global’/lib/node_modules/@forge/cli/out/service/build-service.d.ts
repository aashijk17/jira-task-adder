import { AppConfigProvider } from '@forge/cli-shared';
import { BuildGraphqlClient } from '../build/graphql-client';
export declare const BUILD_TAG_MAX_LENGTH = 64;
export declare const BUILD_TAG_REGEX: RegExp;
export declare type BuildByAppUser = {
    name?: string;
    email?: string;
};
export declare type BuildByApp = {
    createdAt: string;
    tag: string;
    createdBy: BuildByAppUser;
};
export declare class BuildService {
    private readonly getAppConfig;
    private readonly buildClient;
    constructor(getAppConfig: AppConfigProvider, buildClient: BuildGraphqlClient);
    generateBuildTag(): string;
    validateBuildOptions({ buildTag }: {
        buildTag: string;
    }): void;
    getBuildsByApp(limit?: number): Promise<BuildByApp[]>;
}
//# sourceMappingURL=build-service.d.ts.map