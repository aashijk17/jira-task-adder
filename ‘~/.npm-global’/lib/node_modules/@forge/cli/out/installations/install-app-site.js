"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.InstallAppSiteCommand = void 0;
class InstallAppSiteCommand {
    getAppConfig;
    installAppClient;
    constructor(getAppConfig, installAppClient) {
        this.getAppConfig = getAppConfig;
        this.installAppClient = installAppClient;
    }
    async execute({ environmentKey, site, product, licenseOverride, overrides, majorVersionId, environmentType }) {
        const { id: appId } = await this.getAppConfig();
        return await this.installAppClient.installAppIntoSite({
            appId,
            environmentKey,
            site,
            product,
            licenseOverride,
            overrides,
            majorVersionId,
            environmentType
        });
    }
}
exports.InstallAppSiteCommand = InstallAppSiteCommand;
//# sourceMappingURL=install-app-site.js.map