"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAllScopeKeys = exports.extractDomain = void 0;
const extractDomain = (urlAstNode) => {
    try {
        return new URL(urlAstNode.url).hostname;
    }
    catch (e) {
        return urlAstNode.url;
    }
};
exports.extractDomain = extractDomain;
const getAllScopeKeys = (scopes) => (Array.isArray(scopes) ? scopes : Object.keys(scopes));
exports.getAllScopeKeys = getAllScopeKeys;
